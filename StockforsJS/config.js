var config = {
        type: Phaser.AUTO,
        width: 3000,
        height: 3000,
        
        scene: {
            preload: preload,
            create: create,
            update: update
        }
    };

//var game = new Phaser.game(config);





    